# Python environment — regenerate with:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Bundled runtimes (Temurin JDK + QuestDB) and DB data — large, regenerable
runtime/
qdb-data/

# Secrets (compose auto-loads .env; TG_BOT_TOKEN / TG_CHAT_ID live there)
.env

# macOS
.DS_Store

# Claude Code session worktrees
.claude/
